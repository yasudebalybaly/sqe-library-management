# LibraryHub Test Cases

| ID | Title | Requirement | Preconditions | Steps | Expected | Priority | Type | Result / Notes |
|---|---|---|---|---|---|---|---|---|
| TC-001 | Add book with valid new ISBN | REQ-01 | Library is initialized and the ISBN is not already present. | 1. Create a book with valid book information and a new ISBN. 2. Add the book to the library. | The book is added successfully and can be found in the library. | High | Positive / Functional | **PASS** — Equivalent `book_id` functionality successfully added a new book. |
| TC-002 | Reject duplicate ISBN on add_book | REQ-02 | A book with the same ISBN already exists in the catalog. | 1. Add a book with an existing ISBN. 2. Attempt to add another book using the same ISBN. | The second book is rejected and the original catalog entry remains unchanged. | High | Negative / Functional | **PASS** — Equivalent duplicate `book_id` was correctly rejected with `ValueError`. |
| TC-003 | Reject malformed ISBN on add_book | REQ-03 | Library is initialized. | 1. Create a book using a malformed ISBN. 2. Attempt to add the book. | The malformed ISBN is rejected with an appropriate validation error. | High | Negative / Functional | **BLOCKED** — ISBN field and ISBN validation are not implemented. |
| TC-004 | Borrow book when copies are available | REQ-04 | A book exists and has at least one available copy. A valid member exists. | 1. Request the member to borrow the book. | The borrowing operation succeeds and the number of available copies decreases by one. | High | Positive / Functional | **PASS** — Equivalent `issue_book()` operation succeeded and available copies decreased by one. |
| TC-005 | Reject borrowing when no copies are available | REQ-05 | A book exists but has zero available copies. A valid member exists. | 1. Request the member to borrow the unavailable book. | The borrowing operation is rejected and the available-copy count remains zero. | High | Negative / Functional | **PASS** — Equivalent `issue_book()` correctly rejected a book with zero available copies. |
| TC-006 | Return a book currently on loan | REQ-06 | A member currently has the selected book on loan. | 1. Return the book. | The return operation succeeds and the available-copy count increases appropriately. | High | Positive / Functional | **PASS** — Equivalent `return_book()` restored the available copy after issue. |
| TC-007 | Reject return when book is not on loan by member | REQ-06 | The selected book is not currently on loan by the specified member. | 1. Attempt to return the book for that member. | The invalid return operation is rejected and library records remain unchanged. | Medium | Negative / Functional | **BLOCKED** — Member-specific loan tracking is not implemented; `return_book()` accepts only `book_id`. |
| TC-008 | Borrow at the allowed member limit | REQ-07 | A member has borrowed fewer books than the maximum allowed limit. Enough available books exist. | 1. Borrow books until the member reaches the allowed limit. | The final borrowing operation within the limit succeeds. | High | Positive / Functional | **BLOCKED** — `borrow_book()` and member borrowing-limit functionality are not implemented. |
| TC-009 | Reject borrowing beyond allowed member limit | REQ-07 | A member has already reached the maximum borrowing limit. | 1. Attempt to borrow one additional book. | The additional borrowing operation is rejected and the member remains at the allowed limit. | High | Negative / Functional | **BLOCKED** — `borrow_book()` and member borrowing-limit functionality are not implemented. |
| TC-010 | Fine calculation for zero days overdue | REQ-08 | A returned book has zero overdue days. | 1. Calculate the fine for zero overdue days. | The calculated fine is zero. | Medium | Positive / Functional | **BLOCKED** — Fine calculation functionality is not implemented. |
| TC-011 | Fine calculation for mid-range overdue period | REQ-08 | A returned book has a mid-range number of overdue days. | 1. Calculate the fine using the applicable fine rate. | The calculated fine matches the defined fine rule for the overdue period. | Medium | Positive / Functional | **BLOCKED** — Fine calculation functionality is not implemented. |
| TC-012 | Fine calculation at overdue-tier boundary | REQ-08 | A returned book is exactly at an overdue-tier boundary. | 1. Calculate the fine at the boundary value. | The system applies the correct fine tier and returns the expected boundary fine. | Medium | Boundary / Functional | **BLOCKED** — Fine calculation functionality is not implemented. |

